# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# An example controller showing a sample home page
GET        /:query                    controllers.HomeController.index(query:String)
GET        /skills/:jobId         controllers.HomeController.skills(jobId:String)
GET        /user/:userId            controllers.HomeController.user(userId:String)
GET        /stats/:query/:single        controllers.HomeController.stats(query:String, single:String)


# Map static resources from the /public folder to the /assets URL path
GET        /assets/*file        controllers.Assets.versioned(path="/public", file: Asset)
